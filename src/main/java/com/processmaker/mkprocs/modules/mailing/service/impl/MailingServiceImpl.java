package com.processmaker.mkprocs.modules.mailing.service.impl;

import com.processmaker.mkprocs.modules.mailing.service.MailingService;
import com.processmaker.mkprocs.utils.Result;
import org.springframework.stereotype.Service;

@Service
public class MailingServiceImpl implements MailingService {

    public Result readTemplate(String category) {
        Result rst = new Result();

        return rst;
    };
    public Result createTemplate() {
        Result rst = new Result();

        return rst;
    };
}
